<h1>Add something to <%= @cocktail.name %></h1>
<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :ingredient, collection: @ingredients %>
  <%= f.input :description %>

  <%= f.button :submit %>
<% end %>
